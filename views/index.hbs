<p>Welcome to BasserCal</p>
<ul>
	{{#each events}}
	<li>
		<div>
			<h1>{{this.date}}</h1>
			<ul>
				{{#each this.events}}
				<a href="/events/{{this.id}}">{{this.name}}</a>
				{{/each}}
			</ul>
		</div>
	</li>
	{{/each}}
</ul>